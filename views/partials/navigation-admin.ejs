<nav class="fixed-top navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/">A.N</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a href="/admin" class="<%=page==="home" ? "active": ""%> nav-link">Home</a>
                </li>
                <li class="nav-item">
                    <a href="/admin/library" class="<%=page==="library" ? "active": ""%> nav-link">Library</a>
                </li>
            </ul>
            <div class="d-flex">
                <a href="/admin/profile" class="btn <%=page==="profile" ? "btn-light": "btn-outline-light"%>  me-2"><%= user.username%></a>
                <a href="/admin/logout" class="btn btn-outline-danger">Logout</a>
            </div>
        </div>
    </div>
</nav>
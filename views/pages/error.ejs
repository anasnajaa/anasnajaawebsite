<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
    <%- include('../partials/head', {header: {title: pd.error.title, description: pd.error.message}}); %>
</head>

<body>

    <header>
        <%- include('../partials/navigation', {page: pd.error.code, user: pd.user}); %>
    </header>
    <div class="container mt-4">
        <div class="row py-5">
            <div class="col-12 col-sm-12 col-md-8 col-lg-6 col-xl-6 mx-auto text-center">
                <div class="card">
                    <div class="card-body">
                        <h1><%=pd.error.title %></h1>
                        <p><%=pd.error.message %></p>
                        <a class="btn btn-secondary" href="/">Back to home page</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer>
        <%- include('../partials/footer'); %>
    </footer>

</body>

</html>